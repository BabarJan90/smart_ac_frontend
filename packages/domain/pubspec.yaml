name: domain
description: "Domain Module"
version: 0.0.1

environment:
  sdk: 3.11.5
  flutter: 3.41.8

resolution: workspace
dependencies:
  flutter:
    sdk: flutter

  equatable:
  injectable:
  freezed_annotation:

dev_dependencies:

  flutter_test:
    sdk: flutter
  build_runner:
  flutter_lints:
  bloc_test:
  mocktail:
  faker:

  # tools
  freezed:
  injectable_generator:

  # linter
  analyzer: